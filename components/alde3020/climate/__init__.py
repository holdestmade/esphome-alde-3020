# Update for CLIMATE_SCHEMA compatibility

# This change replaces climate.CLIMATE_SCHEMA.extend() with the new approach that uses climate.CLIMATE_SCHEMA.schema_plain
climate.CLIMATE_SCHEMA = climate.CLIMATE_SCHEMA.schema_plain